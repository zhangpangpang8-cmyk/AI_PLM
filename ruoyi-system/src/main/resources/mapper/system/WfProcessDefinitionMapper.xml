<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.WfProcessDefinitionMapper">

    <resultMap type="WfProcessDefinition" id="WfProcessDefinitionResult">
        <id     property="id"                 column="id"                  />
        <result property="processKey"         column="process_key"         />
        <result property="processName"        column="process_name"        />
        <result property="processCategory"    column="process_category"    />
        <result property="processType"        column="process_type"        />
        <result property="bpmnXml"            column="bpmn_xml"            />
        <result property="description"        column="description"         />
        <result property="status"             column="status"              />
        <result property="createBy"           column="create_by"           />
        <result property="createTime"         column="create_time"         />
        <result property="updateBy"           column="update_by"           />
        <result property="updateTime"         column="update_time"         />
        <result property="remark"             column="remark"              />
    </resultMap>

    <sql id="selectWfProcessDefinitionVo">
        select id, process_key, process_name, process_category, process_type, bpmn_xml, description, status,
               create_by, create_time, update_by, update_time, remark
        from wf_process_definition
    </sql>

    <select id="selectWfProcessDefinitionList" parameterType="WfProcessDefinition" resultMap="WfProcessDefinitionResult">
        <include refid="selectWfProcessDefinitionVo"/>
        <where>
            <if test="processKey != null  and processKey != ''"> and process_key = #{processKey}</if>
            <if test="processName != null  and processName != ''"> and process_name like concat('%', #{processName}, '%')</if>
            <if test="processCategory != null  and processCategory != ''"> and process_category = #{processCategory}</if>
            <if test="processType != null  and processType != ''"> and process_type = #{processType}</if>
            <if test="status != null  and status != ''"> and status = #{status}</if>
        </where>
        order by create_time desc
    </select>

    <select id="selectWfProcessDefinitionById" parameterType="Long" resultMap="WfProcessDefinitionResult">
        <include refid="selectWfProcessDefinitionVo"/>
        where id = #{id}
    </select>

    <select id="selectWfProcessDefinitionByKey" parameterType="String" resultMap="WfProcessDefinitionResult">
        <include refid="selectWfProcessDefinitionVo"/>
        where process_key = #{processKey} and status = '0'
    </select>

    <insert id="insertWfProcessDefinition" parameterType="WfProcessDefinition" useGeneratedKeys="true" keyProperty="id">
        insert into wf_process_definition
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="processKey != null and processKey != ''">process_key,</if>
            <if test="processName != null and processName != ''">process_name,</if>
            <if test="processCategory != null and processCategory != ''">process_category,</if>
            <if test="processType != null and processType != ''">process_type,</if>
            <if test="bpmnXml != null and bpmnXml != ''">bpmn_xml,</if>
            <if test="description != null and description != ''">description,</if>
            <if test="status != null and status != ''">status,</if>
            <if test="remark != null and remark != ''">remark,</if>
            <if test="createBy != null and createBy != ''">create_by,</if>
            <if test="createTime != null">create_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="processKey != null and processKey != ''">#{processKey},</if>
            <if test="processName != null and processName != ''">#{processName},</if>
            <if test="processCategory != null and processCategory != ''">#{processCategory},</if>
            <if test="processType != null and processType != ''">#{processType},</if>
            <if test="bpmnXml != null and bpmnXml != ''">#{bpmnXml},</if>
            <if test="description != null and description != ''">#{description},</if>
            <if test="status != null and status != ''">#{status},</if>
            <if test="remark != null and remark != ''">#{remark},</if>
            <if test="createBy != null and createBy != ''">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
        </trim>
    </insert>

    <update id="updateWfProcessDefinition" parameterType="WfProcessDefinition">
        update wf_process_definition
        <trim prefix="SET" suffixOverrides=",">
            <if test="processKey != null and processKey != ''">process_key = #{processKey},</if>
            <if test="processName != null and processName != ''">process_name = #{processName},</if>
            <if test="processCategory != null and processCategory != ''">process_category = #{processCategory},</if>
            <if test="processType != null and processType != ''">process_type = #{processType},</if>
            <if test="bpmnXml != null and bpmnXml != ''">bpmn_xml = #{bpmnXml},</if>
            <if test="description != null and description != ''">description = #{description},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="remark != null and remark != ''">remark = #{remark},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteWfProcessDefinitionById" parameterType="Long">
        delete from wf_process_definition where id = #{id}
    </delete>

    <delete id="deleteWfProcessDefinitionByIds" parameterType="String">
        delete from wf_process_definition where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
